<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Mooville.QUno.Universal"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Class="Mooville.QUno.Universal.SettingsPage"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Resources>
        <x:Double x:Key="SmallWindowBreakpoint">0</x:Double>
        <x:Double x:Key="LargeWindowBreakpoint">479</x:Double>
    </Page.Resources>
    <Grid>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource LargeWindowBreakpoint}"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="sliderDefaultComputerPlayers.Width" Value="384"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource SmallWindowBreakpoint}"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="sliderDefaultComputerPlayers.Width" Value="256"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="0">
            <Button x:Name="buttonBack" Style="{StaticResource NavigationBackButtonNormalStyle}"/>
            <TextBlock Text="Settings" Style="{StaticResource HeaderTextBlockStyle}" VerticalAlignment="Center" Margin="16,0,0,0"/>
        </StackPanel>
        <StackPanel Margin="24" Grid.Row="1" Grid.Column="0">
            <StackPanel Margin="0,0,0,12" MaxWidth="512" HorizontalAlignment="Left">
                <TextBlock Text="New games" Style="{StaticResource SubheaderTextBlockStyle}" Margin="0,0,0,12"/>
                <TextBlock Text="Player name:" Margin="0,0,0,8"/>
                <TextBox x:Name="textDefaultHumanPlayerName" Margin="0,0,0,8 " AccessKey="N"/>
                <TextBlock Text="Number of computer players:" Margin="0,0,0,8"/>
                <Slider x:Name="sliderDefaultComputerPlayers" Minimum="1" Maximum="9" HorizontalAlignment="Left" AccessKey="C"/>
            </StackPanel>
            <StackPanel Margin="0,0,0,12" HorizontalAlignment="Left">
                <TextBlock Text="About this app" Style="{StaticResource SubheaderTextBlockStyle}" Margin="0,0,0,12"/>
                <TextBlock Text="QUno" FontWeight="Bold"/>
                <TextBlock x:Name="textVersion" Text="Version {0}"/>
                <TextBlock Text="Copyright © 2021 Roger Deetz. All rights reserved."/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
